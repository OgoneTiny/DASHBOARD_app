services:
  - type: web
    name: games_analytics
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn dashboard:server"
  git add render.yaml
git commit -m "Add render.yaml for Render deployment"
git push origin main

